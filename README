Application Configuration Managment for Amazon Docker Flow

Linux Install:

$ curl -L drone.io/github.com/ryandotsmith/adf-config/files/adf-config.tar.gz \
  | tar xvz

Usage:

$ adf-config -l -a x
FOO=f
BAR=b

$ adf-config -s BAZ=b -a x
BAZ=b

$ adf-config -d BAZ -a x
BAZ=b
